# 冷媒库接入验收

2026-09-09，Windows 当前工作区；用户要求将 `答复_/5-冷媒库.xlsx` 接入 MCHE，作为可提供和选择的介质目录。

## 已实现

61项冷媒/载冷剂保存为可重建的JSON/CSV，保留序号、类别、精确介质标识、说明、隐藏E列的浓度、质量/体积浓度基准及原单元格证据。原文件未修改，末次读取仍为13353字节，SHA-256 `ef2987e4328be020eff7e1c9a8beedff88ed68b9d0b8460780a6d63fe909cb66`。

MCHE新增四个 `refrigerant_*` 查询/比较/建议工具、同源HTTP入口与用户专用确认。`selections.refrigerant`、候选与部件版本独立保存，和扁管、翅片并存；当前会话、建议版本、目录摘要及确认重放在后端校验。确认后读取目录形成完整不可变快照，旧扁管/翅片记录兼容读取，不回写历史。

右侧增加“冷媒”页签，支持类别浏览、精确标识查询、条件比较、输入核对、确认和来源展示。每轮提供最新冷媒快照摘要，参数准备包含已确认介质及浓度基准。原表序号不当作DLL编码，`dllFluidIdentifier=null`，物性和DLL映射继续阻塞。

使用方式和完整字段见 [冷媒目录](../refrigerant-catalog.md) 与 [MCHE说明](../../packages/dsh-mche/README.md)。

## 自动验证

| 检查 | 结果 |
|---|---|
| 冷媒目录、冷媒服务、原有服务与HTTP定向运行 | 21/21通过 |
| 三份目录 `node --test src/catalogs/refrigerants.test.mjs src/catalogs/fins.test.mjs src/catalogs/flat-tubes.test.mjs` | 16/16通过，无跳过；冷媒315格逐项对账、JSON/CSV重建一致 |
| 首次完整MCHE运行 | 29通过、1失败；新增原生场景的重启后追问返回间歇502 |
| `node --test packages/dsh-mche/test/refrigerant-native.test.js` 独立重跑 | 2/2通过，无跳过 |
| 串行MCHE与模型配置集成 | MCHE全部30/30通过；模型配置1项仍返回502，总计30通过、1失败 |
| `npm test` | 105通过、2失败、2跳过；失败为原有扁管原生流程和模型配置流程的502，新增冷媒测试均通过 |
| `pnpm run typecheck` | 通过 |
| 最后调整草稿工具的部件路由后，冷媒服务与工具/HTTP定向复验 | 7/7通过，类型检查再次通过 |
| `git diff --check` | 已跟踪文件空白检查通过 |

串行命令为 `node --test --test-concurrency=1 packages/dsh-mche/test/*.test.js packages/dsh-model-config/test/runtime.test.js`，并设置 `DSH_TEST_KEEP_FAILED=1` 和前次保留的 `WEB_APP_TEST_ENTRY=.dsh/fin-agent-debug-web.mjs` 用于错误诊断。没有加入自动重试或修改产品保存逻辑。日志分别保留在 `.dsh/refrigerant-mche-test.log`、`.dsh/refrigerant-catalog-test.log`、`.dsh/refrigerant-integration-serial.log`、`.dsh/refrigerant-full-test.log`。

间歇502在此前扁管/翅片阶段已有记录；本次同样未定位底层异常，不声称已经修复或全仓全绿。两个跳过项分别是Linux专用视觉预览和依赖外部旧工程原表的可选集成测试。此次未运行Linux验证。

原生测试使用真实DSH循环与本地确定性模型，覆盖语言/表格草稿、查询/比较/建议、三种确认并存、重复/跨会话拒绝、每轮状态、重启恢复，以及Vol.改Wt.后只撤销冷媒确认。后端测试还覆盖目录变化、旧记录兼容、禁止覆盖浓度、0–100%校验、未知基准、61项分页完整性和三种快照的32KB响应预算。

## 浏览器验收

使用本任务启动的本地隔离DSH服务和确定性模型，通过真实Chrome页面完成以下操作；未修改或重启用户原服务。

1. 对话提出乙二醇体积浓度30%、扁管A01S、翅片B01及管长/管数/管间距，实际产生查询、比较、建议和蓝色“确认冷媒”等入口。
2. 在右侧分别确认EG30Vol.、A01S和B01，三种确认同时保留；冷媒展示序号28、30%、体积基准和原表来源。
3. 在“冷媒 → 输入核对”将浓度基准改为质量浓度并保存，原EG30Vol.需要重新确认，扁管和翅片仍已确认。
4. 对比EG30Vol.与EG30Wt.，前者明确显示浓度基准不满足，后者满足三项明确条件；建议并确认EG30Wt.。
5. 核对输入后准备参数，页面显示扁管逻辑参数完整、翅片核心几何完整、冷媒目录选择完整；冷媒为序号40、30%质量浓度，来源A42:E42，C42为基准依据、E42为浓度。DLL和装配待核实项继续显示。
6. 刷新页面后EG30Wt.、A01S和B01均恢复为已确认，原体积浓度建议的确认按钮失效，不能重复确认。
7. 按“载冷剂·水”浏览仅返回WATER，查看来源显示序号27及A29:E29，E29保持“未提供”；只查看来源没有替换已选EG30Wt.。
8. 检视稳定后的确认页截图，抽屉、来源表、三种页签和确认状态显示正常；浏览器警告/错误日志为空。

## 边界

未验证外部模型提取准确率、物性、材料兼容性、介质适用工况或DLL调用。文件中的介质标识与序号只作为源目录信息；不能据此认定物性库已支持或介质适用。

没有提交或推送代码、目录或TLL记录；没有重启用户原服务。已运行的服务需要重启才能加载新增后端工具。整体MCHE计算任务继续进行，本次完成的范围是冷媒目录与选择接入。
