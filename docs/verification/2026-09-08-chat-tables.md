# 对话与表格阶段验证记录

日期：2026-09-08。工作目录：`E:\projects\dsh_mche_agent`。

## 已验证

- Web App 定向测试 17/17 通过；最终仓库测试中这 17 项也均通过。
- `pnpm run typecheck` 退出 0。该脚本检查现有 TypeScript，新增 JavaScript 由运行测试覆盖。
- `pnpm peers check` 退出 0，无 peer dependency 问题。
- `pnpm install --frozen-lockfile` 退出 0；依赖增量仅 SheetJS 0.20.3，保留其他已锁定版本。
- `git diff --check` 退出 0。
- 新增 table-* 模块通过 AST 定向检查：文件不超过 300 行、函数不超过 50 行、参数不超过 3 个、圈复杂度不超过 10。

测试覆盖 CSV 引号/换行、UTF-8/UTF-16/GB18030、分隔符、空值/零/前导零、重复列、XLSX/XLS、
多工作表、公式文本、隐藏表与合并单元格提示、损坏或超限数据拒绝、真实 HTTP 上传、结构校验、普通对话兼容。

真实 Cordis/Agent 配合本地模拟模型验证：自然语言提取请求、表格确认修改进入模型、系统提示词保持、
会话关闭/恢复、重启恢复及新会话隔离。这些检查曾随全套测试 28/28 通过。

浏览器使用隔离配置和本地模拟模型，已观察：

- Markdown 粘贴 → 草稿 → 修改 16 为 25.4 → 发送 → 回复 → 刷新恢复。
- 自然语言“扁管宽度16mm，开窗角度27度，换热量未提供。” → 参数表 → 修改 → 确认并发送 → 继续对话。
- 刷新后原 AI 表保留 16，新用户表保留 25.4，缺失换热量保持空白。
- 直接通过剪贴板粘贴 Excel TSV 区域，自动生成 2 行 × 3 列草稿。
- 新建对话清空此前消息；浏览器控制台未观察到应用错误。
- 窄屏 DOM 宽度与 scrollWidth 相等，无页面整体横向溢出；完整滚动验收因浏览器 CDP 手势超时未完成。

按 requesting-code-review 的检查维度由主 Agent 完成代码自查，遵守项目单主 Agent 约束。
自查修复了超限文本粘贴被截断、失败会话不能清空输入/放弃草稿、累计表格超限和带引号 CSV 分隔符判断问题。

## 当前未通过项与限制

最终 `pnpm test`：**27 通过、1 失败，退出 1**。
日志：`.dsh/chat-tables-final-tests.log`。失败为 `table-flow.js` 中新会话请求返回 502。
此前也曾在普通聊天或切换模型后触发，具有间歇性，不是每次失败。

独立诊断捕获的根因：`MetadataStore.persist` 原子重命名 `lite-workspace.json.*.tmp` 时，Windows 返回 EPERM。
此时 Agent 的 turn 已完成，保存失败被聊天 HTTP 层映射为 502。
临时诊断打印已移除；未增加生产日志中的用户内容或凭据输出。

准备了 `.dsh/windows-rename-fix.patch`：仅在 Windows 的 EPERM/EACCES/EBUSY 时，对原子替换进行有界重试，
等待时间为 20/40/80/160/320 ms，不删除原文件、不改元数据格式；持续失败仍上报。
补丁的临时重试路径定向验证通过；尚未应用，也未声称持久化问题已解决。
依据用户 AGENTS.md 的持久化改动确认规则，已发出确认请求，当前尚无答复。

浏览器扩展未开启本地文件访问，`fileChooser.setFiles` 返回 `Not allowed`。
Excel/CSV 已通过真实服务上传接口验证；浏览器“选文件到上传完成”的全链路仍待补验，未绕过扩展权限。

尚未调用真实外部模型测提取准确率，未调用 DLL，未完成正式字段映射或完整计算验收。
